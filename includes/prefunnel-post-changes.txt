-- add hidden field stripe_subscription_id
-- date picker html fluent form fields copy










steps to export a funnel from localhost to live
1) export and import the form to live
2) update fluent form id's in the plugin
3) update page form_id in shortcode and shortcode data
4) set constants